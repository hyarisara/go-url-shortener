# Go URL Shortener

A simple CLI application in Go to shorten URLs with persistent storage.

## Usage

```bash
go run . shorten <url>   # Shorten a URL
go run . expand <code>   # Expand a short code
go run . list            # List all stored URLs
